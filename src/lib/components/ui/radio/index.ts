import { RadioGroup as RadioPrimitive } from 'bits-ui';

import Item from './radio-item.svelte';
import Root from './radio.svelte';
const Input = RadioPrimitive.Input;

export { Input, Item, Root as Radio, Input as RadioInput, Item as RadioItem, Root };
